name: Keep Services Alive

on:
  schedule:
    - cron: '0 8 */3 * *'  # A cada 3 dias às 08:00 UTC
  workflow_dispatch:         # Permite executar manualmente

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: 🏓 Ping Railway Backend
        run: |
          echo "Pingando o backend Railway..."
          curl -f https://linkedin-prospector-production.up.railway.app/ping
          echo ""
          echo "✅ Backend e Supabase mantidos ativos!"
